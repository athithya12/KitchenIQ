# --- FRAMEWORK & API ---
Django~=5.0
djangorestframework~=3.15

# --- CORS ---
django-cors-headers~=4.3

# --- ASGI & Channels (Real-Time) ---
channels~=4.0
daphne~=4.1         # ASGI server
channels-redis~=4.2 # Channel layer (requires Redis)

# --- DATABASE & ORM ---
psycopg2-binary~=2.9 # Postgres adapter

# --- AUTHENTICATION ---
supertokens-python~=0.30

# --- STORAGE (S3/MinIO) ---
django-storages~=1.14
boto3~=1.34

# --- UTILITIES ---
django-environ~=0.11 # For managing settings via environment variables